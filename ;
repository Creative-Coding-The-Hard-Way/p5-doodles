const fs = require("fs");
const path = require("path");

module.exports = async () => {
  const sketches_dir = path.join(__dirname, "../../src/sketches/");
  const files = fs.readdirSync(sketches_dir);

  console.log("FOUND FILES ", JSON.stringify(files, undefined, 2));

  return files;
};
